# Values this repository has declared safe to publish.
# One token per line, matched case-insensitively against the exact text the
# scanner flagged. `#` starts a comment. Every entry MUST carry a reason —
# an unexplained allowlist entry is indistinguishable from a mistake.
#
#   python Meta-Brain/tools/scan_public_safety.py .

# Throwaway password for the ephemeral Oracle XE demo container. It is
# published deliberately so the Oracle->PostgreSQL walkthrough is reproducible
# by anyone; the container is local to a demo VM, holds only the HR/PetClinic
# sample schemas, and is destroyed after the demo. Rotate nothing — there is
# nothing to rotate. If port 1521 is ever exposed publicly, this becomes a
# real finding again: parameterise the scripts instead of extending this note.
PASSWORD=Demo_Ora2Pg_2026!

# GitHub user-attachments asset id for the README teaser video
# (https://github.com/user-attachments/assets/4bdba31c-...). It is not a tenant,
# subscription or workspace id: GitHub mints it when a file is uploaded through
# the web UI, and it is the only form GitHub expands into a <video> player.
# It is already public by construction - the README that carries it is public.
# Removing it does not protect anything; it just breaks the player.
4bdba31c-01a6-46a5-a728-76725cbe5aec